#include <stdio.h>

int main (int argc,char *argv[]){
    //no se utiliza mucho
    int edad=45;
    do{
        printf("Tu edad es menor que 60 anos");
    }while(edad<60);
    return 0;
    //no ejecutar porque se queda en bucle
}